import styled from 'styled-components';

export const Container = styled.ul`
    padding: 24px 0 24px 0;
    display: flex;
    
    overflow: scroll;
    overflow-y: hidden;;

    cursor: pointer;

    /* &::-webkit-scrollbar {
        display: none;
    } */
`;